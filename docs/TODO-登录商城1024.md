# 登录 + 商城 1024（v3 基准上的唯一范围）

> **基准**：`v3.0.0` — 场内固定 800×600，`dllmain` 仅 `ApplyNativeResolution()`。  
> **不做**：场内分辨率切换、StatusBar `1024*`、WndMan 锚点、SysOpt 分辨率下拉。

---

## 阶段 0（当前）

- [x] 代码回退 `v3.0.0`
- [x] 踩坑记录见 `docs/分辨率改造踩坑记录.md`
- [x] 拣入 `CashShop.img` 的 `1024backgrnd` 资源（commit `69f0249` 内容）

## 阶段 1 — 商城 1024

1. 进商城时：视口临时 1024×768 + `UpdateResolution` 里**仅商城** codecave 段（`myWidth`/`myHeight`）
2. 出商城回场图：恢复 800×600 + `ApplyNativeResolution`
3. uipath 仅一条：`CashShop.img/Base/backgrnd` → `1024backgrnd`（进商城时生效）
4. 验收：800 场图正常 → 开商城全屏底图不花屏 → 关商城恢复

## 阶段 2 — 登录 1024

1. 登录/选角 Stage：视口 1024×768（若配置开启）
2. uipath：`Login.img/Common/frame` → `1024frame`（需与汉化框对齐，不行则只用 800 frame）
3. 进 Field 前：强制回 800
4. 验收：登录框居中无黄条；进图后底栏与 v3 一致

## 实现约束

- 新代码放独立小文件（如 `CashShop1024.cpp` / `Login1024.cpp`），**不**恢复 `krt/resolution.cpp` 全量 mod
- 每个阶段单独 commit + 你测过再下一步
